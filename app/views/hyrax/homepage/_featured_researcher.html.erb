<%# OVERRIDE / bring-forward of Hyrax/Hyku's themed _featured_researcher partial
    into the knapsack so we can add a "View all researchers" button beneath the
    curated featured-researcher content block. Resolved for the default home
    theme (no theme view path injected); mirrors the theme partials' content.

    @featured_researcher is the admin-curated `featured_researcher` ContentBlock
    (rich text). The button is pure navigation to the contributors browse index;
    it is styled to match the home page "view all collections" button
    (btn btn-secondary mt-1 inside a list-inline). %>
<div class="featured-researcher mb-4 p-2">
  <% if @featured_researcher.value.present? %>
    <%= displayable_content_block @featured_researcher %>
  <% else %>
    <%= t('hyrax.homepage.featured_researcher.missing') %>
  <% end %>

  <ul class="list-inline researcher-highlights-list mt-2">
    <li>
      <%# Literal path matches the knapsack idiom for engine routes (see the
          relationship-map link in _compound_card). %>
      <%= link_to t('enact.homepage.featured_researcher.view_all', default: 'View all research profiles'),
                  '/contributors',
                  class: 'btn btn-secondary mt-1' %>
    </li>
  </ul>
</div>
